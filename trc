- use os.path.join everywhere that a path name is manipulated
- name all of the goddamn things the right way (e.g. ms_test)
- move results folder (all important things, really) into sandbox
- graceful copying of testbed stuff into sandbox
- C# migration?
